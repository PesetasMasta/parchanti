build/
node_modules/
inspiration/
.worktrees/
.superpowers/
dist/
.astro/

# Unverified personal data about named individuals; must not reach the public repo.
bios-draft.md

# Client-facing question list; stays out of the public repo.
QA.md

# Draft of the client e-mail; stays out of the public repo.
email-otazky.md

# Client's raw notes; stays out of the public repo.
FEEDBACK.md

# IG message draft; stays out of the public repo.
ig-zprava.md

# Netlify site link (site ID); local state, not shared.
.netlify/

# Local review sheets: a copy of the built site per variant, rebuilt on
# demand by mockups-build.sh. Never published - publish-docs.sh copies dist/.
mockups/
mockups-build.sh
mockups-variants.py
